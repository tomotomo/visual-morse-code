# ADR01: レスポンシブ対応の改善案

## 背景
UIレビューの結果、PC表示では概ね1画面に収まる一方、タブレットおよびモバイルでは縦方向のオーバーフローが発生している。

## 現状の課題
- タブレット表示: 画面高さを超えて約268px のオーバーフロー
- モバイル表示: 画面高さを超えて約517px のオーバーフロー
- SVGエリアが固定高さであるため、画面サイズ縮小時に余白や縦方向の負担が大きい
- `body` に `overflow: hidden` が設定されており、画面外要素の表示を阻害しやすい

## 改善案
1. タブレット/モバイル向けにSVGエリアの高さを縮小する
   - 例: `min-height: 320px`〜`420px`
2. `body` のスクロール制御を見直す
   - `overflow-x: hidden; overflow-y: auto;` に変更し、縦スクロールを許容する
3. モバイル向けの余白と要素間隔を圧縮する
   - ボタン領域・入力領域・出力領域の間隔を詰める
4. SVGエリアを `aspect-ratio` ベースで可変にする
   - 固定 `min-height` 依存を避け、画面幅に応じた高さ調整にする
5. タブレット/モバイルで表示順を再検討する
   - 必要に応じてコントロールを上段にまとめ、縦の情報密度を下げる

## 優先度
- 高: 1, 2, 4
- 中: 3, 5

## 期待効果
- タブレット・モバイルで1画面内に収めやすくなる
- スクロールが必要な場合でも自然に見える
- 小さい画面でも操作性が向上する
