# ADR-01: 練習モードを基盤に、ゲームモードを追加できる構成へ移行する

- ステータス: 提案中
- 日付: 2026-05-24

## コンテキスト

- 既存の `index.html` は、UI、Morseデコード、オーディオ再生、SVG描画、タイマー制御が単一のHTMLに集約されていた。
- 現在の実装は練習用途の操作に最適化されているが、ゲームモード追加を前提にすると責務が肥大化しやすい。
- `spec.md` は「単一ファイルSPAとしての仕様」を前提としているが、最近の変更でモジュール分割を試みており、設計の前提が変わっている。

## 決定

- 練習モードを主導線とするアプリ構成に移行し、ゲームモードは別モジュールとして追加できる構成へ変更する。
- 既存の機能は以下のように責務分割する。
  - `index.html`: 画面の骨格とモジュール読み込みのみを担う
  - `app.js`: 初期化とモードの入口
  - `practice-mode.js`: 練習モードのUI・入力・タイマー・描画
  - `morse-engine.js`: Morseツリー、デコード、パス検証
  - `audio-player.js`: AudioContext と再生制御
- モード切り替えは、まずは単純な画面切替の形で実装し、段階的にゲームモードを追加する。

## 理由

- 1ファイルに責務が集中すると、ゲーム追加時に条件分岐と副作用が増える。
- 共通ロジック（Morseデコード、音声再生、ツリー描画）を分離しておくと、練習モードとゲームモードの両方で再利用できる。
- `spec.md` の前提は「単一ファイルの実装」であり、今回の分割方針は仕様の根本を変えるため、判断理由を残す必要がある。

## 影響

- HTMLから埋め込みスクリプトを削除し、ES modules に移行する。
- 既存の練習モードの動作は維持したまま、拡張しやすい構造にする。
- 今後ゲームモードを追加する際は、`game-mode.js` のような専用モジュールを増やせる。

## トレードオフ

- メリット:
  - 責務分離ができる
  - 再利用性が高くなる
  - ゲームモード追加の足場になる
- デメリット:
  - 実装の初期コストが増える
  - モジュール読み込みの管理が必要になる
  - まだゲームモードの具体的なUI/UXは未定である

## 代替案

- 単一HTMLのままゲームモードを追加する案
  - 短期的にはシンプルだが、責務が肥大化しやすい
- 先にフレームワークを導入する案
  - 現時点では過剰で、変更規模が大きすぎる

## 検討事項

- ゲームモードは「別画面」か「同一画面のタブ切替」かを後続で判断する
- 文字列の問題生成、スコア計算、正答率の算出方法を別ADRで定義する
- 既存のオーディオ再生とハイライト挙動をゲーム側でも再利用するかを検討する
