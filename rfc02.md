# RFC02: 実装品質と失敗耐性の改善案

## 背景
コードレビューの結果、見た目・操作性は十分に良い一方で、ユーザー操作に対する失敗耐性と保守性に弱点が見えている。特に、誤入力時の復帰、音声再生の競合、回帰テストの欠如が、実運用での不具合リスクを高める。

## 現状の課題
1. **不正入力時の状態が回復しにくい**
   - 不正なモールスパスを入力した場合、エラー表示は出るが、状態が明確に復旧しない。
   - `Clear` を使わない限り、同じ状態から抜けられない可能性がある。

2. **入力と再生の競合が発生しやすい**
   - `playCurrentOutput()` で再生中でも、新しい入力操作がそのまま音声再生を開始できる。
   - 連続入力時に音声が重なり、学習体験を損ねる。

3. **回帰テストが存在しない**
   - 主要な挙動（入力、確定、クリア、再生、エラー表示）を自動で検証する仕組みがない。
   - 小さな変更で UI や状態の破綻を見逃すリスクが高い。

4. **SVG描画の実装が読みづらい**
   - `renderTree()` 内で親ノード探索を毎回行っており、描画ロジックの意図が分かりにくい。
   - 可読性と保守性の観点で改善余地がある。

5. **未使用のスタイル・定義が残っている**
   - `glow` フィルタが定義されているが、実際には反映されていない。
   - `#output-text` への参照が残っており、現在の DOM 構成と齟齬がある。

## 改善案
1. **不正入力時の復旧ルールを明確化する**
   - 不正な入力時は `Current Input` をエラー表示し、3秒の回復猶予を設ける。
   - その間に有効な入力が入った場合は、その入力で状態を復帰させる。
   - 3秒経過しても入力が続かなければ、入力バッファを自動で安全にリセットする。
   - これは、実際のモールス運用で誤りを認識してから再送する流れに近く、ユーザーが「何が起きたか」を理解しやすい。

2. **再生の競合を防止する**
   - 入力操作または `playCurrentOutput()` の開始時に、既存の再生を停止する。
   - `AudioPlayer` 側で再生状態を管理し、重複再生を防ぐ。

3. **最小限の自動テストを追加する**
   - `createPracticeMode()` の主要フローを対象に、
     - 入力→確定
     - 不正入力→エラー
     - `Clear` による状態リセット
     - 再生開始／停止
     を検証するテストを追加する。

4. **SVG描画ロジックを単純化する**
   - 親ノード検索をやめ、再帰の引数として親ノードを渡す形に整理する。
   - 描画ロジックを `renderTree()` の責務に寄せ、探索処理と描画処理を分離する。

5. **未使用の CSS / SVG 定義を削除する**
   - 未使用の `glow` 定義を削除する。
   - 現在の DOM に存在しない `#output-text` 参照を削除する。

## 優先度
- 高: 1, 2, 3
- 中: 4
- 低: 5

## 期待効果
- 不正入力時の UX が改善され、ユーザーが迷いにくくなる
- 音声再生の競合が解消され、学習体験が安定する
- 変更時の誤動作を早期に検知できるようになり、保守性が向上する
- SVG描画コードが読みやすくなり、今後の改修が容易になる
- 不要な定義や古い CSS を除去でき、実装の意図が明確になる

## 補足
本案は、既存の UI デザインや操作感を保ったまま、実際の利用時に起こりやすい不具合を減らすことを目的としている。特に、ユーザーから見た「壊れている」と感じる挙動を先に潰す方針で整理した。